parameters:
  # dump the Service Container into a Single File
  app_db: "%env(string:DATABASE_URL)%"
doctrine:
  dbal:
    # url: '%app_db%'
    # local
      url: 'mysql://rooti:rooti@127.0.0.1:3306/jj-software?serverVersion=mariadb-10.6.3'